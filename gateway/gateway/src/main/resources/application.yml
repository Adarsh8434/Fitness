server:
  port: 8085

spring:
  application:
    name: api-gateway

  config:
    import: optional:configserver:http://localhost:8080
logging:
  level:
    org.springframework.boot.context.properties: TRACE